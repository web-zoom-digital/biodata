/** A4 at 96 dpi. Every template renders into this fixed box; exports scale it up. */
export const A4_WIDTH = 794;
export const A4_HEIGHT = 1123;

export const FONT_DISPLAY = "var(--font-display), Georgia, 'Times New Roman', serif";
export const FONT_BODY = "var(--font-body), 'Segoe UI', Roboto, Arial, sans-serif";
